module.exports = {
    // The root directory that Jest should scan for test files
    rootDir: './backend/tests',
    // The directories where Jest should look for modules
    moduleDirectories: ['node_modules'],
};